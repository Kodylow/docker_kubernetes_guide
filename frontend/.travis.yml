language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t kodylow/docker_kubernetes_guide -f Dockerfile.dev .

script:
  - docker run -e CI=true kodylow/docker_kubernetes_guide npm run test
